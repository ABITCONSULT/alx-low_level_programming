#include <stdio.h>
/**
 * main-entry point for the program printf
 *
 * Return:zero if no error and non-zero if errors
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
